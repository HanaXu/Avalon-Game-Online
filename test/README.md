## How to use
```
Install Anaconda w/ Python 3 https://www.anaconda.com/distribution/

Open Jupyter Notebooks App

In Jupyter Notebooks, click new -> terminal

In the Jupyter terminal, type in 'conda install selenium'

After installation, browse to and open the .ipynb notebook file

Start the vue app & start the server

Then run each block using shift+enter
```